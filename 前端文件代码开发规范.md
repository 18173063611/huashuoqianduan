# AI 数字人视频网站前端文件代码开发规范

本文档用于约束 `huashuoqianduan` 前端开发。项目当前按 Vue3、Node22、TypeScript、Vite 和统一 CSS 样式进行开发。所有页面围绕 AI 数字人视频制作工作台展开。**数据访问由后端完成：服务端持久层采用 MyBatis 与 MyBatis-Plus（详见《后端文件代码开发规范》），前端仅通过 HTTP 调用后端接口，禁止直连数据库。**

## 1. 开发人员与文件归属

前端由费文轩、黄宝泉共同开发，同一时间内不得二人同时修改同一文件。**文件与资源命名须准确反映业务职责与功能语义**；维护责任以业务域划分、飞书任务卡与代码评审结论为准。

| 人员 | 主要前端职责 |
| --- | --- |
| 费文轩 | 基础页面框架、项目列表、声音与 TTS、形象生成、标题封面、前后端联调 |
| 黄宝泉 | 任务中心、资产中心、视频解析、文案结构化结果、视频合成进度 |

协作规则：
1. **按业务域拆目录**：`pages/project / pages/task / pages/asset ...`、`services/*`、`types/*` 等按业务域组织，禁止按人员拆目录。
2. **按功能命名文件**：业务页面、业务组件、业务 API、业务 store、业务类型等文件须以业务功能命名，名称应直接体现职责与边界，例如 `ProjectWorkbench.vue`、`taskApi.ts`、`assetTypes.ts`。
3. **每个文件唯一维护人**：谁创建的业务文件，谁维护；另一人不得直接修改该文件。确需修改时，先在飞书任务卡说明原因、修改范围、影响页面并得到确认。
4. **公共/核心抽象单负责人**：入口（如 `main.ts`）、布局骨架（如 `layout/*`）、通用请求封装、通用类型等公共核心文件也按功能命名，例如 `request.ts`、`apiTypes.ts`；必须在任务卡里明确唯一负责人。
5. **禁止双人同时改同一页面链路**：同一功能不允许两个人同时改同一个页面、组件、API 文件、状态文件或路由文件。

## 1.1 业务域负责人边界（精简版）

前端按业务域划分负责人，避免跨域修改同一文件：

| 业务域 | 负责人 | 边界说明 |
| --- | --- | --- |
| `project`、`voice`、`avatar`、`publish`、基础工作台与联调页 | 费文轩 | 基础页面框架、项目管理、声音/TTS、形象、标题封面 |
| `task`、`asset`、`render`、`video`、`script` | 黄宝泉 | 任务中心、资产中心、视频解析/脚本结构化展示、视频合成进度 |

## 2. 前端页面范围

MVP 核心页面：
1. 项目列表
2. 视频解析
3. 文案改写
4. 分镜脚本
5. 声音选择
6. 形象生成
7. 视频合成
8. 标题封面
9. 资产中心
10. 任务中心

工作台布局：
1. 左侧可折叠菜单栏。
2. 中间为主要操作区。
3. 右侧或顶部展示任务状态、生成进度、预览入口。
4. 所有生成结果必须可以保存、查看、预览和复用。

## 3. 前端目录建议

推荐按业务域组织目录，不按人员单独拆目录。文件名体现功能，负责人记录在任务卡和业务域边界中。

```text
src/
├── app/
├── assets/
├── components/
│   ├── layout/
│   ├── ui/
│   └── business/
├── pages/
│   ├── project/
│   ├── video/
│   ├── script/
│   ├── voice/
│   ├── avatar/
│   ├── render/
│   ├── publish/
│   ├── asset/
│   └── task/
├── services/
├── stores/
├── router/
├── types/
└── utils/
```

职责边界：
1. `project`：项目列表、项目详情、项目创建，默认费文轩负责。
2. `video`：视频链接解析、原文案展示、基础数据展示，默认黄宝泉负责。
3. `script`：文案改写、分镜脚本、AI 结构化结果展示，默认黄宝泉负责。
4. `voice`：声音选择、试听、TTS 提交、音频预览，默认费文轩负责。
5. `avatar`：形象上传、形象生成、参考图预览，默认费文轩负责。
6. `render`：视频合成参数、字幕、BGM、音量、生成进度，默认黄宝泉负责。
7. `publish`：标题、简介、话题、封面生成，默认费文轩负责。
8. `asset`：资产中心列表、筛选、预览、复用，默认黄宝泉负责。
9. `task`：任务中心、任务状态、重试、失败原因，默认黄宝泉负责。

## 4. 文件命名规范

业务域新增文件必须使用功能前缀。Vue 组件使用 PascalCase，TypeScript、样式、工具文件使用 kebab-case 或 camelCase。

公共/核心文件（入口、布局、通用 UI、通用请求封装等）也使用功能命名，并指定唯一负责人维护。

### 4.1 项目与生成能力命名示例

```text
ProjectList.vue
ProjectCreateDialog.vue
VoiceSelect.vue
TtsPanel.vue
AvatarUpload.vue
PublishPack.vue
projectApi.ts
voiceApi.ts
avatarApi.ts
publishTypes.ts
useProjectStore.ts
```

### 4.2 任务与资产命名示例

```text
TaskCenter.vue
TaskProgress.vue
AssetCenter.vue
VideoParse.vue
ScriptRewrite.vue
RenderPanel.vue
taskApi.ts
assetApi.ts
renderApi.ts
workflowTypes.ts
useTaskStore.ts
```

### 4.3 命名反例（应避免）

```text
TempProjectList.vue
GenericTaskCenter.vue
AssetApi.ts
types.ts
utils.ts
```

说明：应避免含义模糊、无法体现业务域或模块职责的命名；类型与工具文件宜带业务前缀（如 `assetTypes.ts`），避免单文件承担过多无关职责。

## 5. Vue 代码规范

Vue 文件统一使用 Composition API 和 `<script setup lang="ts">`。

推荐结构：

```vue
<template>
  <section>
    <!-- 页面内容 -->
  </section>
</template>

<script setup lang="ts">
// imports
// types
// props / emits
// state
// computed
// methods
// lifecycle
</script>
```

要求：
1. 页面组件只负责页面状态和用户交互，不直接写复杂业务算法。
2. API 请求统一放到 `services` 目录。
3. 类型定义统一放到 `types` 目录；业务域类型文件名必须体现功能，例如 `projectTypes.ts`、`taskTypes.ts`。
4. 组件 props 和 emits 必须定义 TypeScript 类型。
5. 禁止在模板中写复杂表达式。
6. 禁止在组件内硬编码后端地址。
7. 大于 300 行的页面应拆分为子组件，子组件仍需按功能命名。

## 6. 中文注释规范

前端代码必须在关键交互和数据流处添加中文注释，帮助前后端联调时快速理解页面行为。

要求：
1. 注释使用中文，说明“为什么这样处理”或“这段交互保护什么用户流程”。
2. API 请求封装、任务轮询、上传流程、页面联动、状态切换、错误处理等关键逻辑必须有必要注释。
3. 简单模板展示、普通变量声明、样式类名不需要注释。
4. 注释必须和代码保持一致，修改交互逻辑时同步修改注释。
5. 禁止写无意义注释，例如“设置变量”“调用接口”“返回数据”。

## 7. UI 与样式规范

界面风格保持工作台形态，优先复用统一 UI 组件。

样式要求：
1. 当前阶段统一使用 `src/styles/app-style.css` 管理工作台基础样式。
2. 通用 UI 组件放在 `components/ui`。
3. 业务组件放在 `components/business` 或对应业务页面目录。
4. 颜色、圆角、间距保持统一，不在每个页面随意发明新样式。
5. 主要操作按钮放在页面右上角或当前操作区底部。
6. 任务进度、失败原因、预览入口必须明显展示。
7. 未引入 Tailwind CSS 或 shadcn/ui 前，不得在规范或代码中假定这些依赖已经可用。

## 8. API 调用规范

前端调用后端统一前缀：

```text
/api/v1
```

API 文件按业务拆分，文件名使用功能前缀：

```text
projectApi.ts
voiceApi.ts
avatarApi.ts
publishApi.ts
uploadApi.ts
videoApi.ts
scriptApi.ts
taskApi.ts
assetApi.ts
renderApi.ts
```

接口函数命名：

```ts
export function getProjectList() {}
export function createProject() {}
export function createRenderTask() {}
export function getTaskDetail() {}
```

要求：
1. 页面组件不直接调用 `fetch` 或 `axios`。
2. 所有接口请求必须通过 service 函数。
3. 所有接口响应必须使用统一响应类型。
4. 接口错误必须显示明确提示。
5. 异步任务接口提交成功后，前端必须保存 `taskId` 并轮询或订阅任务状态。

## 9. 统一响应类型

后端统一返回：

```ts
export interface ApiResponse<T> {
  code: number
  message: string
  data: T
  traceId: string
}
```

分页类型：

```ts
export interface PageResult<T> {
  records: T[]
  pageNo: number
  pageSize: number
  total: number
}
```

前端处理规则：
1. `code === 0` 才认为成功。
2. `code !== 0` 时展示 `message`。
3. 调试问题时把 `traceId` 提供给后端。
4. 不允许页面直接依赖后端未定义字段。

## 10. 类型定义规范

类型文件按业务域和功能命名。

```text
projectTypes.ts
voiceTypes.ts
avatarTypes.ts
publishTypes.ts
taskTypes.ts
assetTypes.ts
renderTypes.ts
workflowTypes.ts
```

命名示例：

```ts
export interface ProjectItem {}
export interface CreateProjectRequest {}
export interface TaskItem {}
export interface AssetItem {}
```

要求：
1. 请求类型以 `Request` 结尾。
2. 响应类型以 `Response` 或 `Item` 结尾。
3. 枚举值必须与后端约定一致。
4. 任务状态、资产类型、生成类型必须使用联合类型或枚举，不使用散乱字符串。

## 11. 路由规范

路由路径使用小写中划线：

```text
/projects
/upload
/video-parse
/script-rewrite
/storyboard
/voice
/avatar
/render
/publish
/assets
/tasks
```

路由文件如需拆分，使用功能前缀：

```text
projectRoutes.ts
voiceRoutes.ts
taskRoutes.ts
assetRoutes.ts
```

要求：
1. 路由标题必须与左侧菜单一致。
2. MVP 页面可以先用静态菜单，但路径不能随意改。
3. 当前阶段可以使用工作台 `activeKey` 控制页面切换；引入 Vue Router 后再落地为真实路由。
4. 页面路径或菜单 key 一旦被联调使用，修改前必须同步后端和产品验收人。

## 12. 状态管理规范

项目状态、任务状态、资产状态可以使用 Pinia 或 Vue 自身状态管理，但状态文件必须按业务拆分并使用功能前缀。

```text
useProjectStore.ts
useVoiceStore.ts
useTaskStore.ts
useAssetStore.ts
```

要求：
1. 页面局部状态留在组件内。
2. 跨页面共享状态放入 store。
3. 任务轮询状态统一由任务相关 store 管理。
4. 不同业务模块不能互相直接修改对方 store。
5. 需要跨模块协作时通过明确函数或事件传递。

## 13. 异步任务前端规范

以下功能必须按任务处理：
1. 视频链接解析。
2. 文案分析和改写。
3. 分镜脚本改写。
4. TTS 音频生成。
5. 形象生成。
6. 数字人视频合成。
7. 标题封面生成。

任务状态展示：

```text
QUEUED      排队中
RUNNING     处理中
SUCCESS     成功
FAILED      失败
RETRYABLE   可重试
```

前端要求：
1. 提交生成任务后立即展示任务卡片。
2. 任务处理中展示进度或加载状态。
3. 任务成功后展示预览入口和资产入口。
4. 任务失败后展示失败原因和重试按钮。
5. 不允许用户重复快速点击造成多个重复任务，按钮需要 loading 或禁用。

## 14. 资产展示规范

所有生成结果必须进入资产中心并支持预览。

资产类型：

```text
TEXT
IMAGE
AUDIO
VIDEO
COVER
JSON
```

资产中心页面至少支持：
1. 按项目筛选。
2. 按资产类型筛选。
3. 查看生成时间。
4. 查看来源任务。
5. 预览图片、音频、视频、文案和封面。
6. 复用历史资产。

## 15. 表单规范

表单要求：
1. 必填字段必须有明显标识。
2. 提交前做基础校验。
3. 后端返回错误时显示 `message`。
4. 提交中按钮必须进入 loading 状态。
5. 生成类表单必须展示预计耗时或任务状态提示。
6. 文案、Prompt、脚本等大文本字段必须支持复制。

## 16. 交互与验收规范

每个页面完成后必须满足：
1. 能打开页面。
2. 能看到空状态。
3. 能看到加载状态。
4. 能看到成功状态。
5. 能看到失败状态。
6. 能根据后端返回的 `traceId` 协助排查问题。
7. 生成类结果可以保存并在资产中心看到。

MVP 阶段优先保证主流程跑通，不追求复杂动画和高级编辑器。

## 17. 环境变量规范

环境变量文件：

```text
.env.local
.env.development
.env.production
.env.redis.local
.env.upload.local
```

要求：
1. 后端接口地址使用环境变量。
2. 本地个人配置不得提交；需要共享的环境配置按环境或功能命名。
3. 禁止提交真实密钥。
4. 前端不得保存第三方 API Key，第三方调用必须走后端。

## 18. 提交前检查清单

每次提交前必须确认：
1. 命名符合第 4 节约定的业务与功能语义。
2. 没有修改对方负责文件。
3. 页面可以正常打开。
4. 接口路径、请求字段、响应字段与后端一致。
5. 异步任务有 loading、成功、失败和重试展示。
6. 生成结果可以进入资产中心或至少保留资产入口。
7. 没有提交密钥、Token、本地临时文件。
8. 本地构建或类型检查通过。

## 19. 联调约定

前端联调前必须准备：
1. 页面路径。
2. 调用的接口路径。
3. 请求参数示例。
4. 需要后端返回的字段。
5. 页面成功态截图或说明。
6. 页面失败态处理方式。
7. 任务型接口的 `taskId` 查询逻辑。

接口字段一旦被页面使用，前后端都不能单方面修改字段名、字段类型或状态枚举。必须先同步任务卡，再统一修改。
