# AI 数字人视频网站前端文件代码开发规范

本文档用于约束 `huashuoqianduan` 前端开发。项目当前按 Vue3、Node22、TypeScript、Vite、Tailwind CSS、shadcn/ui 风格组件进行开发。所有页面围绕 AI 数字人视频制作工作台展开。

## 1. 开发人员与文件归属

前端由费文轩、黄宝泉共同开发，但同一时间不能编辑同一个文件。每个新增文件必须能从文件名看出负责人，避免多人协作冲突。

| 人员 | 文件名标识 | 主要前端职责 |
| --- | --- | --- |
| 费文轩 | `fwx` | 基础页面框架、项目列表、声音与 TTS、形象生成、标题封面、前后端联调 |
| 黄宝泉 | `hbq` | 任务中心、资产中心、视频解析、文案结构化结果、视频合成进度 |

协作规则：
1. 新建文件必须包含 `fwx` 或 `hbq` 标识。
2. 费文轩创建和维护的文件只能由费文轩修改，黄宝泉创建和维护的文件只能由黄宝泉修改。
3. 如必须修改对方文件，先在飞书任务卡说明原因、修改范围、影响页面，得到确认后再改。
4. 公共入口文件必须提前指定唯一负责人，文件名也要带负责人标识或在任务卡中明确当天维护人。
5. 禁止两个人同时编辑同一个页面、组件、API 文件、状态文件或路由文件。

## 2. 前端页面范围

MVP 核心页面：
1. 项目列表
2. 视频解析
3. 文案改写
4. 分镜脚本
5. 声音选择
6. 形象生成
7. 视频合成
8. 标题封面
9. 资产中心
10. 任务中心

工作台布局：
1. 左侧可折叠菜单栏。
2. 中间为主要操作区。
3. 右侧或顶部展示任务状态、生成进度、预览入口。
4. 所有生成结果必须可以保存、查看、预览和复用。

## 3. 前端目录建议

推荐按业务域组织目录，不按人员单独拆目录。人员标识体现在文件名中。

```text
src/
├── app/
├── assets/
├── components/
│   ├── layout/
│   ├── ui/
│   └── business/
├── pages/
│   ├── project/
│   ├── video/
│   ├── script/
│   ├── voice/
│   ├── avatar/
│   ├── render/
│   ├── publish/
│   ├── asset/
│   └── task/
├── services/
├── stores/
├── router/
├── types/
└── utils/
```

职责边界：
1. `project`：项目列表、项目详情、项目创建，默认费文轩负责。
2. `video`：视频链接解析、原文案展示、基础数据展示，默认黄宝泉负责。
3. `script`：文案改写、分镜脚本、AI 结构化结果展示，默认黄宝泉负责。
4. `voice`：声音选择、试听、TTS 提交、音频预览，默认费文轩负责。
5. `avatar`：形象上传、形象生成、参考图预览，默认费文轩负责。
6. `render`：视频合成参数、字幕、BGM、音量、生成进度，默认黄宝泉负责。
7. `publish`：标题、简介、话题、封面生成，默认费文轩负责。
8. `asset`：资产中心列表、筛选、预览、复用，默认黄宝泉负责。
9. `task`：任务中心、任务状态、重试、失败原因，默认黄宝泉负责。

## 4. 文件命名规范

所有新增文件必须包含 `fwx` 或 `hbq`。Vue 组件使用 PascalCase，TypeScript、样式、工具文件使用 kebab-case 或 camelCase，但必须包含人员标识。

### 4.1 费文轩文件示例

```text
FwxProjectList.vue
FwxProjectCreateDialog.vue
FwxVoiceSelect.vue
FwxTtsPanel.vue
FwxAvatarUpload.vue
FwxPublishPack.vue
fwxProjectApi.ts
fwxVoiceApi.ts
fwxAvatarApi.ts
fwxPublishTypes.ts
useFwxProjectStore.ts
```

### 4.2 黄宝泉文件示例

```text
HbqTaskCenter.vue
HbqTaskProgress.vue
HbqAssetCenter.vue
HbqVideoParse.vue
HbqScriptRewrite.vue
HbqRenderPanel.vue
hbqTaskApi.ts
hbqAssetApi.ts
hbqRenderApi.ts
hbqWorkflowTypes.ts
useHbqTaskStore.ts
```

### 4.3 禁止命名

```text
ProjectList.vue
TaskCenter.vue
AssetApi.ts
request.ts
types.ts
utils.ts
```

原因：无法判断负责人，容易造成两个人同时编辑同一个文件。

## 5. Vue 代码规范

Vue 文件统一使用 Composition API 和 `<script setup lang="ts">`。

推荐结构：

```vue
<template>
  <section>
    <!-- 页面内容 -->
  </section>
</template>

<script setup lang="ts">
// imports
// types
// props / emits
// state
// computed
// methods
// lifecycle
</script>
```

要求：
1. 页面组件只负责页面状态和用户交互，不直接写复杂业务算法。
2. API 请求统一放到 `services` 目录。
3. 类型定义统一放到 `types` 目录，文件名必须带 `fwx` 或 `hbq`。
4. 组件 props 和 emits 必须定义 TypeScript 类型。
5. 禁止在模板中写复杂表达式。
6. 禁止在组件内硬编码后端地址。
7. 大于 300 行的页面应拆分为子组件，但子组件仍需带负责人标识。

## 6. shadcn/ui 与样式规范

界面风格保持工作台形态，优先复用统一 UI 组件。

样式要求：
1. 优先使用 Tailwind CSS 原子类。
2. 通用 UI 组件放在 `components/ui`。
3. 业务组件放在 `components/business` 或对应业务页面目录。
4. 颜色、圆角、间距保持统一，不在每个页面随意发明新样式。
5. 主要操作按钮放在页面右上角或当前操作区底部。
6. 任务进度、失败原因、预览入口必须明显展示。

## 7. API 调用规范

前端调用后端统一前缀：

```text
/api/v1
```

API 文件按业务拆分，文件名必须带人员标识：

```text
fwxProjectApi.ts
fwxVoiceApi.ts
fwxAvatarApi.ts
fwxPublishApi.ts
hbqVideoApi.ts
hbqScriptApi.ts
hbqTaskApi.ts
hbqAssetApi.ts
hbqRenderApi.ts
```

接口函数命名：

```ts
export function fwxGetProjectList() {}
export function fwxCreateProject() {}
export function hbqCreateRenderTask() {}
export function hbqGetTaskDetail() {}
```

要求：
1. 页面组件不直接调用 `fetch` 或 `axios`。
2. 所有接口请求必须通过 service 函数。
3. 所有接口响应必须使用统一响应类型。
4. 接口错误必须显示明确提示。
5. 异步任务接口提交成功后，前端必须保存 `taskId` 并轮询或订阅任务状态。

## 8. 统一响应类型

后端统一返回：

```ts
export interface ApiResponse<T> {
  code: number
  message: string
  data: T
  traceId: string
}
```

分页类型：

```ts
export interface PageResult<T> {
  records: T[]
  pageNo: number
  pageSize: number
  total: number
}
```

前端处理规则：
1. `code === 0` 才认为成功。
2. `code !== 0` 时展示 `message`。
3. 调试问题时把 `traceId` 提供给后端。
4. 不允许页面直接依赖后端未定义字段。

## 9. 类型定义规范

类型文件必须带负责人标识。

```text
fwxProjectTypes.ts
fwxVoiceTypes.ts
fwxAvatarTypes.ts
fwxPublishTypes.ts
hbqTaskTypes.ts
hbqAssetTypes.ts
hbqRenderTypes.ts
hbqWorkflowTypes.ts
```

命名示例：

```ts
export interface FwxProjectItem {}
export interface FwxCreateProjectRequest {}
export interface HbqTaskItem {}
export interface HbqAssetItem {}
```

要求：
1. 请求类型以 `Request` 结尾。
2. 响应类型以 `Response` 或 `Item` 结尾。
3. 枚举值必须与后端约定一致。
4. 任务状态、资产类型、生成类型必须使用联合类型或枚举，不使用散乱字符串。

## 10. 路由规范

路由路径使用小写中划线：

```text
/projects
/video-parse
/script-rewrite
/storyboard
/voice
/avatar
/render
/publish
/assets
/tasks
```

路由文件如需拆分，必须带负责人标识：

```text
fwxProjectRoutes.ts
fwxVoiceRoutes.ts
hbqTaskRoutes.ts
hbqAssetRoutes.ts
```

要求：
1. 路由标题必须与左侧菜单一致。
2. MVP 页面可以先用静态菜单，但路径不能随意改。
3. 页面路径一旦被联调使用，修改前必须同步后端和产品验收人。

## 11. 状态管理规范

项目状态、任务状态、资产状态可以使用 Pinia 或 Vue 自身状态管理，但状态文件必须按业务拆分并带负责人标识。

```text
useFwxProjectStore.ts
useFwxVoiceStore.ts
useHbqTaskStore.ts
useHbqAssetStore.ts
```

要求：
1. 页面局部状态留在组件内。
2. 跨页面共享状态放入 store。
3. 任务轮询状态统一由任务相关 store 管理。
4. 不同业务模块不能互相直接修改对方 store。
5. 需要跨模块协作时通过明确函数或事件传递。

## 12. 异步任务前端规范

以下功能必须按任务处理：
1. 视频链接解析。
2. 文案分析和改写。
3. 分镜脚本改写。
4. TTS 音频生成。
5. 形象生成。
6. 数字人视频合成。
7. 标题封面生成。

任务状态展示：

```text
QUEUED      排队中
RUNNING     处理中
SUCCESS     成功
FAILED      失败
RETRYABLE   可重试
CANCELLED   已取消
```

前端要求：
1. 提交生成任务后立即展示任务卡片。
2. 任务处理中展示进度或加载状态。
3. 任务成功后展示预览入口和资产入口。
4. 任务失败后展示失败原因和重试按钮。
5. 不允许用户重复快速点击造成多个重复任务，按钮需要 loading 或禁用。

## 13. 资产展示规范

所有生成结果必须进入资产中心并支持预览。

资产类型：

```text
TEXT
IMAGE
AUDIO
VIDEO
COVER
JSON
```

资产中心页面至少支持：
1. 按项目筛选。
2. 按资产类型筛选。
3. 查看生成时间。
4. 查看来源任务。
5. 预览图片、音频、视频、文案和封面。
6. 复用历史资产。

## 14. 表单规范

表单要求：
1. 必填字段必须有明显标识。
2. 提交前做基础校验。
3. 后端返回错误时显示 `message`。
4. 提交中按钮必须进入 loading 状态。
5. 生成类表单必须展示预计耗时或任务状态提示。
6. 文案、Prompt、脚本等大文本字段必须支持复制。

## 15. 交互与验收规范

每个页面完成后必须满足：
1. 能打开页面。
2. 能看到空状态。
3. 能看到加载状态。
4. 能看到成功状态。
5. 能看到失败状态。
6. 能根据后端返回的 `traceId` 协助排查问题。
7. 生成类结果可以保存并在资产中心看到。

MVP 阶段优先保证主流程跑通，不追求复杂动画和高级编辑器。

## 16. 环境变量规范

环境变量文件：

```text
.env.local
.env.development
.env.production
.env.fwx.local
.env.hbq.local
```

要求：
1. 后端接口地址使用环境变量。
2. 本地个人配置文件必须带 `fwx` 或 `hbq`。
3. 禁止提交真实密钥。
4. 前端不得保存第三方 API Key，第三方调用必须走后端。

## 17. 提交前检查清单

每次提交前必须确认：
1. 文件名包含 `fwx` 或 `hbq` 标识。
2. 没有修改对方负责文件。
3. 页面可以正常打开。
4. 接口路径、请求字段、响应字段与后端一致。
5. 异步任务有 loading、成功、失败和重试展示。
6. 生成结果可以进入资产中心或至少保留资产入口。
7. 没有提交密钥、Token、本地临时文件。
8. 本地构建或类型检查通过。

## 18. 联调约定

前端联调前必须准备：
1. 页面路径。
2. 调用的接口路径。
3. 请求参数示例。
4. 需要后端返回的字段。
5. 页面成功态截图或说明。
6. 页面失败态处理方式。
7. 任务型接口的 `taskId` 查询逻辑。

接口字段一旦被页面使用，前后端都不能单方面修改字段名、字段类型或状态枚举。必须先同步任务卡，再统一修改。
