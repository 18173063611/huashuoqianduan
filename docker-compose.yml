services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:?Set VITE_API_BASE_URL in .env}
        VITE_WS_BASE_URL: ${VITE_WS_BASE_URL:-}
    container_name: huashuo-frontend
    restart: unless-stopped
    ports:
      - "80:80"
